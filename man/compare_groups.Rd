% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/compare_groups.R
\name{compare_groups}
\alias{compare_groups}
\title{Compare numeric scores between two groups by feature or annotation}
\usage{
compare_groups(
  data,
  metadata,
  sample_col,
  group_col,
  group_by = NULL,
  feature_col = NULL,
  annotation_col = NULL
)
}
\arguments{
\item{data}{A numeric matrix or data.frame with rows as features (e.g. metabolites, genes).}

\item{metadata}{A data.frame with sample annotations (e.g. group assignments).}

\item{sample_col}{Name of the column in `metadata` containing sample IDs (string).}

\item{group_col}{Name of the column in `metadata` containing group labels (string).}

\item{group_by}{Optional: If provided, a data.frame with feature annotations (e.g. pathway, subsystem).
Must contain columns: `feature` and `group`.}

\item{feature_col}{Optional: Column name in `group_by` that contains feature IDs.}

\item{annotation_col}{Optional: Column name in `group_by` with groupings (e.g. subsystems).}
}
\value{
A tibble with summary statistics and p-values per feature or group.
}
\description{
General-purpose comparison of numeric values between two groups.
}
\examples{
# Example with synthetic data
set.seed(123)
data_mat <- matrix(rnorm(100), nrow = 10,
                   dimnames = list(paste0("F", 1:10), paste0("S", 1:10)))
metadata <- data.frame(sample = paste0("S", 1:10),
                       group = rep(c("A", "B"), each = 5))

compare_groups(data = data_mat,
                       metadata = metadata,
                       sample_col = "sample",
                       group_col = "group")
}
