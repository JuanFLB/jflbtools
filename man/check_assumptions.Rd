% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/check_assumptions.R
\name{check_assumptions}
\alias{check_assumptions}
\title{Check Normality and Homogeneity Assumptions for Paired Comparisons}
\usage{
check_assumptions(
  data,
  group_col = "group",
  sample_col = "sample",
  label_col = "label",
  value_col = "score",
  label_ref = NULL,
  verbose = FALSE,
  return_table = TRUE
)
}
\arguments{
\item{data}{A data frame.}

\item{group_col}{Name of the grouping column (e.g., cluster, patient group).}

\item{sample_col}{Name of the sample ID column.}

\item{label_col}{Name of the paired condition column (e.g., before/after).}

\item{value_col}{Name of the numeric value column to test.}

\item{label_ref}{Optional: reference level for the `label_col`. If NULL, uses alphabetical order.}

\item{verbose}{Logical; print outlier and QQ plots (default = FALSE).}

\item{return_table}{Logical; include a formatted `gt` table in the returned object (default = TRUE).}
}
\value{
A list with:
\item{df}{tibble with normality and homogeneity test results}
\item{table}{(optional) gt table summary}
}
\description{
Performs Shapiro-Wilk and Levene's tests within each group for paired data.
Returns both a raw results tibble and an optional formatted `gt` table.
}
\examples{
df <- tibble::tibble(
  group = rep(c("A", "B"), each = 6),
  sample = rep(paste0("S", 1:3), each = 2, times = 2),
  label = rep(c("T1", "T2"), times = 6),
  score = stats::rnorm(12)
)
result <- check_assumptions(df, group_col = "group", sample_col = "sample",
                            label_col = "label", value_col = "score")
result$df
result$table

}
